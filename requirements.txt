# -----------------------------------------------------------------------------------------
# [檔案功能說明]
# 這份檔案稱為 `requirements.txt`，是 Python 專案的標準相依性設定檔。
# 它的功能是列出此專案運作所需的所有外部套件 (Packages) 及其精確版本。
#
# [符號說明]
# == : 代表「嚴格鎖定版本」。例如 `torch==2.8.0` 表示必須安裝 2.8.0 版，差一點都不行。
#      這樣做的目的是為了「可重現性 (Reproducibility)」，避免因為套件升級導致舊程式碼壞掉。
# -----------------------------------------------------------------------------------------

# --- Binance 官方交易 SDK (USDⓈ-M Futures) ---
# [套件說明] binance-sdk-derivatives-trading-usds-futures
# 這是 Binance 官方提供的軟體開發套件 (SDK)，專門用於「U本位合約 (USDS-M Futures)」交易。
# 我們的程式需要透過它來跟交易所溝通（下單、查餘額、抓K線）。
# 設定版本 1.5.0 是為了確保 API 介面穩定，不會因為官方更新而導致連線失敗。
binance-sdk-derivatives-trading-usds-futures==1.5.0

# --- 數據分析與繪圖 ---
# [套件說明] matplotlib
# 這是 Python 最老牌且強大的繪圖庫。
# 用途：我們用它來畫出回測結果的 PnL (損益) 曲線、K線圖，或是訓練模型時的 Loss 變化圖，讓我們能直觀地分析數據。
matplotlib==3.8.4 # 或最新穩定版

# --- 深度學習 & 時序 ---
# [套件說明] torch (PyTorch)
# 這是目前最流行的深度學習框架之一 (由 Facebook/Meta 開發)。
# 用途：它是我們建構 AI 模型 (神經網絡) 的核心地基，負責處理張量運算 (Tensor Computing) 和自動微分。
torch==2.8.0

# [套件說明] pytorch-forecasting
#這是一個建立在 PyTorch 之上的高階庫，專門針對「時間序列預測 (Time Series Forecasting)」設計。
# 用途：它封裝了許多專門預測股價或趨勢的模型 (如 Temporal Fusion Transformer)，讓我們不用從零開始刻模型。
pytorch-forecasting==1.4.0

# --- 實驗管理 ---
# [套件說明] mlflow
# 這是一個 MLOps (機器學習維運) 工具。
# 用途：當我們在訓練 AI 模型時，會嘗試幾十種不同的參數。Mlflow 幫我們把每一次訓練的結果 (準確率、Loss) 記錄下來，方便我們比較哪一次的模型最好。
mlflow==3.3.2

# [套件說明] optuna
# 這是一個「超參數最佳化 (Hyperparameter Optimization)」框架。
# 用途：AI 模型有很多設定 (如學習率、層數)。手動調整很累，Optuna 可以自動幫我們「試錯」，找出效果最好的參數組合。
optuna==3.6.1   # 這是比較常見穩定版（你可以確認最新）

# --- 回測 ---
# [套件說明] backtesting
# 這通常指的是 `backtesting.py` 這個輕量級的回測框架。
# 用途：在不花真錢的情況下，用歷史數據模擬交易策略的表現，看看策略是否賺錢。
backtesting==0.6.5  # 若版本衝突可查看 PyPI 最新

# --- 技術指標 ---
# [套件說明] pandas_ta
# 這是一個擴充 Pandas 功能的技術分析庫。
# 用途：它可以直接對 DataFrame 計算數百種技術指標 (如 RSI, MACD, 布林通道)，非常方便。
pandas_ta==0.4.71b0

# --- 通知 (Telegram) ---
# [套件說明] python-telegram-bot
# 這是 Telegram 的機器人介面庫。
# 用途：讓程式可以傳訊息到我們的手機 (Telegram App)。例如：交易成交時通知、或是程式出錯時發送警報。
python-telegram-bot==22.3

# --- 工具類 ---
# [套件說明] python-dotenv
# 這是一個環境變數管理工具。
# 用途：資安考量，我們不應該把 API Key 直接寫在程式碼裡。這個工具讓我們把 Key 存在 `.env` 檔案中，程式執行時再讀取，避免外洩。
python-dotenv==1.0.0

# [套件說明] schedule
#這是一個輕量級的任務排程庫。
# 用途：設定定時任務。例如：「每小時執行一次 K 線抓取」或「每天早上 8 點重新訓練模型」。
schedule==1.2.0

# --- 測試框架 ---
# [套件說明] pytest
# 這是 Python 的單元測試框架。
# 用途：用來執行我們寫的測試程式 (如 test_main.py)，確保程式邏輯正確，沒有 Bug。
pytest

# [套件說明] pytest-mock
# 這是 pytest 的擴充套件。
# 用途：提供 `mock` 功能，讓我們在測試時可以「模擬」交易所的回傳資料，而不用真的去連線 (這對測試非常重要)。
pytest-mock